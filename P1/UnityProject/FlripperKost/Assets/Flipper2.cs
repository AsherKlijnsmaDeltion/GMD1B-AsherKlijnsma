using UnityEngine;
using System.Collections;

public class Flipper2 : MonoBehaviour
{
    public float flipperPower;
    public float forcer;

    // Use this for initialization
    void Start()
    {

    }

    // Update is called once per frame
    void FixedUpdate()
    {
        if (Input.GetButtonDown("Q"))
        {
            

        }
    }
}